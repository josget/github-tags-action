name: Deploy Jotto
on:
  push:
    tags:
      - 'jotto**staging'
      - 'all-**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploying
        run: |
          echo "Deploying reach labs"
          echo "Tag name from github.ref_name: ${{  github.ref_name }}"